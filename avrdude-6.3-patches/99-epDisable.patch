--- usb_libusb.c	2018-01-22 11:30:04.372250879 +0700
+++ "usb_libusb (copy).c"	2018-01-22 11:30:15.048215471 +0700
@@ -246,7 +246,7 @@
 		    }
 
 		  fd->usb.handle = udev;
-		  if (fd->usb.rep == 0)
+		  //if (fd->usb.rep == 0)
 		    {
 		      /* Try finding out what our read endpoint is. */
 		      for (i = 0; i < dev->config[0].interface[iface].altsetting[0].bNumEndpoints; i++)
